{
  "name": "news-script-ai",
  "private": true,
  "description": "AI-powered news article generation platform with Google News 2026 SEO formula & 3-slide quick news generator",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "install-all": "cd frontend && npm install && pip install -r api/requirements.txt"
  },
  "dependencies": {}
}
